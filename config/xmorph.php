<?php

declare(strict_types=1);

return [

    /*
    |--------------------------------------------------------------------------
    | Blade component alias
    |--------------------------------------------------------------------------
    |
    | Tag used to render the package's internal morph component inside
    | author views: `<x-{alias}>`. Rename it when `morph` collides with an
    | app component, e.g. `'alias' => 'as-child'` gives `<x-as-child>`.
    |
    */

    'alias' => 'morph',

];
